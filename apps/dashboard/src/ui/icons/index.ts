/**
 * Icon proxy. All icons are imported from '@icons', never from 'lucide-react'
 * directly, so the icon library can be swapped in exactly one place.
 */
export {
  Activity,
  AlertCircle,
  AlertTriangle,
  ArrowLeft,
  Bell,
  Check,
  CheckCircle2,
  ChevronRight,
  Inbox,
  Layers,
  Loader2,
  Mail,
  MessageSquare,
  Plus,
  RefreshCw,
  Route,
  Send,
  Settings,
  Trash2,
  Users,
  X,
  XCircle,
  Zap,
} from 'lucide-react';
